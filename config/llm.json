{
  "provider": "openai",
  "base_url": "http://localhost:8000/v1",
  "base_urls": [
    "http://localhost:8000/v1",
    "http://localhost:8001/v1"
  ],
  "model": "Qwen3.6-35B-A3B-UD-Q4_K_M.gguf",
  "api_key_env": "",
  "temperature": 0,
  "top_k": 1,
  "top_p": 1.0,
  "min_p": 0.0,
  "seed": 42,
  "max_tokens": 4096,
  "discovery_max_tokens": 8192,
  "pc_max_tokens": 8192,
  "timeout_seconds": 300,
  "retry_attempts": 3,
  "batch_delay_ms": 0,
  "parallel_workers": 1,
  "context_length": 32768,
  "entity_refresh_interval": 50,
  "entity_refresh_batch_size": 10,
  "checkpoint_interval": 25,
  "dedup_audit_interval": 50,
  "skip_response_format": true,
  "entity_detail_raw_instrumentation": true,
  "_comment_entity_detail_raw_instrumentation": "Default ON (#484/#485). Measures TRUE pre-compaction raw_tokens for the entity_detail phase by serialising the full uncompacted prior entity state alongside the compacted prompt actually sent (measurement-only; the prompt sent to the model and the extraction output are unchanged). Set false to skip the extra per-entity json.dumps on long sessions with large catalog entries, where it can become a per-turn CPU/memory bottleneck; when off, entity_detail raw_tokens falls back to the compressed size (pre-#484 behaviour) and compression_ratio returns to 1.0 for that phase.",
  "context_optimizations": {
    "relationship_type_tiering": true,
    "_comment": "L1+L4 type-tiered PC relationship cap (epic #477). Default ON: caps PC entity-detail relationships by type: permanent bonds kept uncapped, volatile tail trimmed to pc_rel_volatile_tail_cap. Also promotes permanent-bond types in the relationship-mapper budget trim. Set false to restore pre-#477 behaviour (byte-identical to the pre-#477 baseline -- i.e. main before #477 -- when the flag is absent or false; note that once this default-ON change merges, main itself ships the flag ON); a same-commit flag-OFF run serves as the A/B control.",
    "pc_rel_permanent_types": ["kinship", "adversarial", "mentorship", "political", "partnership"],
    "pc_rel_volatile_tail_cap": 10,
    "relevance_shadow_logging": false,
    "_comment_relevance_shadow_logging": "S0 relevance-scoped entity_detail shadow (epic #477, lever (b)/A2a, #494). Default OFF and MEASUREMENT-ONLY: when on, each turn's extraction log gets an extra `relevance_detail_shadow` record comparing the actual cap-6 detail set against the set the existing relevance signal (_select_context_aware_entities, #233) WOULD pick -- including the cap-dropped-but-relevant count and the critical safety metric referenced_but_would_drop_count (entities referenced this turn that relevance would drop; must be ~0 for the signal to be sound). It NEVER changes which entities are detailed, the prompts, the LLM calls, or any extracted output: flag-OFF and flag-ON produce byte-identical extraction results, only the extra log stream differs. This S0 shadow validates the relevance signal before the S1 (A2a) behaviour-changing slice gates DetailSet on it.",
    "raw_io_capture": false,
    "_comment_raw_io_capture": "RAW-IO capture instrumentation (epic #477, step 1). Default OFF and MEASUREMENT-ONLY: when on, each LLM call in the extraction pipeline (entity_detail, discovery, relationship_mapper, event) tees one JSONL record to framework/raw-io-capture.jsonl containing the VERBATIM prompt (system+user), the VERBATIM un-parsed completion, the phase, turn id, entity id (for per-entity phases like entity_detail/PC), and per-call input/output token counts (output = decode tokens, the never-before-logged 87%-wall-clock driver; taken from the backend usage.completion_tokens when present, else a char-heuristic estimate flagged *_tokens_estimated=true). It NEVER changes the prompts, the LLM calls, the parsing, or any extracted output: flag-OFF and flag-ON produce byte-identical extraction results and flag-OFF writes NO artifact and does zero capture work. Its purpose is to VALIDATE on real model output (not a parsed-catalog proxy) what entity_detail actually emits, and to expose per-entity PC-call-isolated input tokens. Strict bool parse (is true). Exercise it with one flag-on extraction run to produce the data; it needs no token A/B to merge.",
    "batch_entity_detail": {
      "enabled": false,
      "batch_size": 4,
      "high_confidence_threshold": 0.8,
      "_comment": "L2 batched entity_detail (epic #477, #491). Default OFF: when on, the entity-detail system template + turn text are sent ONCE per batch instead of once per entity, detailing multiple low-salience entities per shared-context call with hard-delimited per-entity blocks. PC (char-player), brand-new entities, and entities with confidence >= high_confidence_threshold stay SOLO to avoid cross-entity attribute bleed; only the lower-salience tail is batched in groups of batch_size. Parse failure or any missing/invalid entity falls back to a solo call (no dropped entities). Flag-OFF is byte-identical to the per-entity baseline (the A/B control)."
    },
    "relevance_scoped_detail": {
      "enabled": false,
      "max_detail_entities": 10,
      "mode": "recover_only",
      "_comment": "S1 relevance-scoped entity_detail selection (epic #477, lever (b)/A2a, #498; builds on the S0 shadow #494). Default OFF and byte-identical to the cap-6 baseline when off (the A/B control). When on in mode 'recover_only' (S1a), the detail set becomes the cap-6 set UNION the relevance recoveries -- cap-dropped entities the existing #233 relevance signal (_select_context_aware_entities) marks relevant (PC, new, or mentioned/co-located/one-hop-active) -- so the ~245 real updates the cap-6 silently drops are recovered. Recover-only is a SUPERSET of the cap-6 set: it ADDS recoveries and drops NOTHING currently detailed. max_detail_entities is the ceiling (telemetry-derived from S0 would_select p95 = 10, max = 18; caps the climax spike, binds on < 5% of turns); on bind it trims the lowest-relevance recovery tail, never the cap-6 set and never the floor (PC/new/mentioned). It is clamped >= the hard cap (_MAX_DETAIL_ENTITIES_PER_TURN = 6). The 'mode' field is the forward-compat seam for S1b ('drop_filler', not implemented here); an unrecognised mode normalises to 'recover_only'. S1 is gated on a QUALITY update-retention A/B vs a cap-6 control, NOT the 60h token A/B (its token delta is ~flat by design)."
    }
  }
}
